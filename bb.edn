{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([procore_construction.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'procore_construction.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
